ARG IMAGE_BUILD
ARG IMAGE_FINAL

FROM $IMAGE_BUILD
ARG WORKDIR
WORKDIR $WORKDIR
COPY . .
RUN c++ main.cpp -static -o app

FROM $IMAGE_FINAL
ARG WORKDIR
WORKDIR /
COPY --from=0 $WORKDIR/app .
CMD ["./app"]

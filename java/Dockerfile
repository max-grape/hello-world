ARG IMAGE_BUILD
ARG IMAGE_FINAL

FROM $IMAGE_BUILD
ARG WORKDIR
WORKDIR $WORKDIR
COPY . .
RUN javac src/Main.java

FROM $IMAGE_FINAL
ARG WORKDIR
WORKDIR /
COPY --from=0 $WORKDIR/src/Main.class .
CMD ["java", "Main"]
